{
  "name": "flow-coverage-diff",
  "version": "0.0.3",
  "description": "Compares your PR flow coverage to the destination branch.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt"
  },
  "author": "sam-com",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0"
  }
}
